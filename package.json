{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^6.5.1"
  },
  "scripts": {
    "lint:fix": "lerna run lint:fix --parallel",
    "install:dev": "lerna bootstrap --ignore=dependencies"
  },
  "devDependencies": {
    "eslint": "^8.36.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.27.5"
  }
}
